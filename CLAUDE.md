## 프로젝트 개요

주제: 5PL 미들마일 플랫폼의 동적 매칭 및 잉여배분 (APIEMS 2026)
구현 언어: Python
현재 단계: 소규모 예비 실험 (품목 6, 공급자 6, 대체 공급자 2 (공급자 수는 시장 조건과 무관하게 고정), 주문당 2~3품목, 기간 20~30, 정책 비교 반복 30)

플랫폼은 다품종 묶음 주문을 내는 주문자와 품목별 공급 용량을 가진 공급자를 매칭하고,
단일 허브로 통합 배송한다. 매 기간 (1) 주문 수락, (2) 품목별 공급자·수량 배정,
(3) 주문자·공급자·플랫폼 간 거래잉여 배분을 결정한다. 주문은 요청한 모든 품목의
요구수량이 확보될 때만 성립한다. 참여자의 다음 기간 재참여 확률은 해당 기간 배분받은 잉여에 따라 달라진다.

연구 질문: 현재의 매칭·잉여배분이 미래 참여자 구성을 바꾸는 플랫폼에서,
미래 가치를 어떻게 근사해야 동적 의사결정이 개선되는가?

방법: 시뮬레이션 경험으로 미래 가치 V(s)를 학습(선형 / MLP / GNN) →
참여자별 유지 가치 c_i = V(s) − V(s에서 참여자 i 제외) (주문자·공급자 모두) →
MILP 목적함수 = 당기 이윤 + Σ c_i × 재참여확률_i(배분 잉여) (재참여확률은 구간선형 근사).
경험 수집과 시뮬레이션 기준치의 후속 정책은 규칙 기반 잉여배분 정책(최선 공급자 몫 비율)이다.
근시안(유지 가치 0)은 잉여를 전부 플랫폼이 가져가 참여자 대부분이 떠나므로, 미래를 전혀 고려하지 않을 때의 참고용 하한으로만 둔다.

## 용어

- 시장 요약 지표: 주문자·공급자 정보를 합계·평균·개수로 요약한 값 (utils/features.py)
  (주문자 수, 공급자 수, 품목별 총 요구량·총 공급가능량·평균 제안가격·평균 공급가격·대체 공급자 수, 주문자·공급자 평균 재참여확률)
- 거래 연결 구조: 어느 주문자가 어느 공급자와 어떤 품목으로 거래할 수 있는지의 연결 관계 (그래프)
- 시장 조건: 시장 구조를 바꾸는 세 설정. 공급 편중도, 대체 공급자 수, 주문당 품목 수 (utils/params.py의 Cfg 필드)
- 공급 편중도 (conc, 0~1): 공급자–품목 연결을 공급자들이 얼마나 고르게 나눠 갖는지. 0이면 모든 공급자의 품목 수가 같고, 높을수록 소수 공급자에게 몰린다 (공급자당 평균 품목 수 = 품목 수 × n_alt / 공급자 수는 고정)
- 대체 공급자 수 (n_alt): 한 품목을 공급할 수 있는 공급자 수
- 주문당 품목 수 (items_per_order): 주문 하나에 묶인 품목 수
- 참여자별 유지 가치 (c_i): V(s) − V(s에서 참여자 i 제외). 참여자 i가 다음 기간에도 남아 있을 때의 미래 가치 기여
- 시뮬레이션 기준치: 같은 상태에서 같은 후속 정책(규칙 기반, 최선 비율)으로 여러 번 시뮬레이션해 얻은 평균 누적 이윤. 예측 모델의 오차를 재는 기준 (match/mc_value.py)

## 실험 구성

- 실험 1 (선형 근사의 한계와 학습된 근사의 필요성):
  같은 시장 요약 지표를 줄 때, 선형 근사(기존 ADP)와 신경망 근사의 미래 가치 예측 정확도와
  장기 성과는 어떻게 다른가?
  → 검증할 가설: 지표들 사이에 조합 효과가 있다면 신경망의 예측 오차가 더 작고 장기 성과가 높다.
  → 반대 결과의 의미: 차이가 없으면 이 환경의 미래 가치는 선형으로 충분히 설명된다.
  → 측정: 시뮬레이션 기준치 대비 예측 오차, 유지 가치 변환 후 추가 오차, 누적 이윤·주문 충족률·참여자 유지율

- 실험 2 (반응 함수에 대한 강건성):
  재참여 반응이 로지스틱이 아닐 때 학습된 근사의 성능은 어떻게 변하는가?
  → 두 경우: 바뀐 반응에 맞춰 재학습 / 로지스틱으로 학습하고 다른 반응 환경에서 평가
  → 결과 해석: 재학습 시 유지되면 방법이 특정 반응 가정에 묶이지 않음을,
    불일치 시 저하 폭은 반응 모형 추정이 얼마나 중요한지를 보여준다. 저하가 크든 작든 그대로 보고한다.

- 실험 3 (구조 정보의 유효 조건):
  시장 요약 지표만 쓰는 신경망(MLP)과, 거래 연결 구조를 그래프로 반영한 신경망(GNN)의
  성과는 어떤 시장 구조 조건에서 달라지는가?
  → 검증할 가설: 공급 편중도를 올릴수록, 대체 공급자가 적을수록, 주문당 품목 수가 많을수록
    구조 정보의 효과가 커진다. 공급 편중도는 수준별 추세로 검증한다.
  → 대조 조건 (차이가 없어야 함): 주문당 품목 수 1. 공급자 구조는 그대로 두고 주문만 단품으로 바꾼 시장이다.
    묶음 주문이 없으면 공급자가 떠날 때의 손해가 품목별로 따로 더해지므로, 시장 요약 지표만으로 충분하고
    거래 연결 구조가 주는 추가 정보가 없다. 이 조건에서 두 방법의 차이는 통계적으로 구분되지 않아야 하며,
    차이가 나면 해석 전에 구현 차이(파라미터 수, 학습량)를 먼저 점검한다.
  → 공급 편중도 0은 대조 조건이 아니라 기준 수준이다. 모든 공급자가 같은 수의 품목을 맡아도
    어떤 품목끼리 같은 공급자에 묶이는지가 남아 있어서 차이가 날 수 있다.
  → GNN이 "MLP + 편중도 요약" 비교군보다 나은지가 구조 정보 효과의 판단 기준이다.
  → 반대 결과의 의미: 모든 조건에서 차이가 없으면, 유지 가치 변환에서 구조 정보가 소실되었거나
    이 규모에서는 시장 요약 지표만으로 충분하다는 뜻이며, 둘 중 무엇인지 변환 오차로 구분한다.
  → 핵심 설계: n_alt 2 고정, 공급 편중도 {0, 0.5, 1} × 주문당 품목 수 {1, 2, 3}.
  → 보조 설계: n_alt 3. 공급자 수가 고정이므로 공급자당 평균 품목 수(2 → 3)가 함께 바뀌어
    대체 공급자 수의 효과와 섞인다는 점을 결과에 명시한다.
  → n_alt 1은 공급 편중도를 바꿀 수 없어(모든 공급자가 1품목) 실험 3에서 제외한다.
    대조 조건으로도 쓰지 않는다: 대체 공급자가 없는 극단적인 시장이라, 차이가 없게 나와도
    구조 정보가 없어서인지 시장이 극단적이어서인지 구분할 수 없다.
  → 측정: 누적 이윤·주문 충족률·참여자 유지율, 유지 가치 변환 오차

비교 기준: 규칙 기반 잉여배분(사전에 정한 고정 비율로 배분. 주문자·공급자 몫 4×4 격자 튜닝: 기준 몫 (0.3, 0.3)보다 튜닝용 반복에서 유의하게 나은 후보가 있을 때만 바꿈.
가치 근사 정책의 주 비교 기준),
근시안 정책(유지 가치 0, 미래를 전혀 고려하지 않을 때의 참고용 하한),
선형 가치 근사(기존 ADP: 시장 요약 지표의 선형 결합으로 미래 가치 근사),
MLP + 편중도 요약(시장 요약 지표에 공급자별 품목 수의 최댓값·분산을 더한 MLP, 실험 3의 GNN 비교군)

## 모델 가정

- 참여자는 고유 프로필을 가진다. 주문자: 품목 구성, 품목별 수량, 제안가격 수준.
  공급자: 공급 품목, 품목별 공급량, 가격 수준. 재참여 시 프로필에 작은 변동을 더해 주문·공급한다.
- 공급자 가격 수준은 실험 3부터 (품목, 그 품목의 k번째 공급자)마다 뽑는다 (Cfg.price_rank = True): 공급자마다 하나를 뽑으면 품목별 평균 공급가격에
  편중도 신호가 새어 "요약 지표가 같고 구조만 다른 시장"이 깨진다. 실험 1까지(diag_*, exp1_*)는 기존 방식(공급자마다 하나)으로 고정해 재현한다.
- 주문당 품목 수 k를 바꿔도 주문자 수와 공급(공급자별 품목, 품목별 총 공급용량)은 같다.
  품목당 주문 수량 범위를 k_ref/k배(k_ref = 2)로 조정해 주문 총량과 품목별 기대 수요를 맞춘다
  (k = 1: 12~30, k = 2: 6~15, k = 3: 4~10, 기간당 신규 주문자 평균 10, 안정 상태 품목별 수요 70).
- 품목별 총 공급용량 = 1.3 × 안정 상태 수요 ÷ 공급자 자리 점유율 = 1.3 × 70 ÷ 0.602 ≈ 151. 점유율은 가정값이 아니라
  기준 몫 (0.3, 0.3)으로 운영한 튜닝용 반복에서 한 번 실측해 고정한 값이며, 기대 실효 공급(용량 × 점유율)의 여유가 1.3이 된다.
- 빈 공급자 자리는 매 기간 확률 0.25로 채워진다 (빈자리 평균 4기간). 현실에서 새 공급자를 플랫폼에 들이는 데(검증·계약·등록) 시간이 걸리고,
  충원이 빠르면 공급자를 잃는 손해가 없어 유지 가치가 의미를 잃기 때문이다. 슬라이스 2 비교군 결과를 본 뒤 0.5에서 바꿨지만 현실 근거에 따른 모델 가정 변경이다.
- 주문 한 건은 품목 공급량의 작은 몫이다: k = 1 주문 한 건의 평균 수량(21)이 품목별 총 공급용량(151)의 1/4 이하.
- 재참여 확률은 해당 기간 배분 잉여에만 의존한다. 거래 이력(충성도 효과)은 이번 범위에 포함하지 않는다.
- 재참여 확률 = 로지스틱(잉여율), 잉여율 = 배분 잉여 / 제안 금액(주문자 Σ 제안가격×수량, 공급자 Σ 공급가격×공급가능량).
  로지스틱을 두 점으로 직접 정한다 (주문자·공급자 동일): 잉여 0이면 0.5(거래하지 못해도 절반은 남음), 기준 잉여율(기준 몫 (0.3, 0.3)으로
  마진의 30%를 받을 때의 평균 잉여율, 튜닝용 반복 실측: 주문자 0.1048, 공급자 0.1020)이면 0.85. 이분탐색 보정은 쓰지 않는다.
  이유: 잉여 0에서 0.1이면 거래하지 못한 참여자의 90%가 떠나 시장이 무너지며, 이는 현실적이지 않다.
  운영 정책의 실제 재참여율은 목표가 아니라 결과로 보고한다 (과정은 ref/plan/roadmap.md 슬라이스 2 결정 기록).
- 잉여율의 공급자 분모(env/platform.worth)는 실제로 판 양이 아니라 그 기간에 내놓은 공급가능량 전체의 금액(Σ 공급가격×공급가능량)이다: 유휴 용량이 많은 공급자는 같은 잉여에도 잉여율이 낮아 떠나기 쉽다.
- 플랫폼 이윤 ≥ 0 제약은 개별 주문이 아니라 기간 합 기준이다: 같은 기간 안의 교차 보조를 허용해, 유지 가치를 위해 개별 거래에서 손해를 감수할 수 있다 (기간을 넘는 적자는 허용하지 않음).
- 한 주문의 한 품목은 여러 공급자가 나눠 공급할 수 있다(허브에서 모아 통합 배송). 배송비는 성립 주문마다 허브→주문자 고정비 1회만 두고 개당 비용은 없다(수량 비례 운송·작업비는 제안가격과 공급가격의 차이에 반영된 것으로 봄), 분할 공급에 추가 비용은 없다.

## 그래프 설계 (vfa/gnn.py)

- 노드: 주문자(품목별 요구수량, 품목별 제안가격, 재참여확률), 공급자(품목별 공급가능량, 품목별 가격, 재참여확률), 노드 종류 표시
- 연결선: 주문자가 원하는 품목을 공급자가 하나 이상 공급할 수 있을 때
- 참여자 수가 기간마다 달라지므로 최대 참여자 수만큼 자리를 두고 활성 마스크로 처리
- 참여자별 유지 가치 c_i = V(G) − V(G에서 노드 i 제거)
- 단계적으로 확장: (1) 연결선 = 거래 가능 여부만 → (2) 연결선에 품목별 거래 가능 여부 추가
  → (3) 연결선에 품목별 잠재 잉여(제안가격 − 공급가격) 추가
- 연결선 정보를 추가할 때마다 시장 요약 지표에도 같은 정보의 합계·평균 버전을 추가해 MLP와 정보량을 맞춘다

## 파일 구조

src/utils: params(Cfg), common(난수 스트림), arrivals, instance, pwl, features
src/env: platform(Env), graph, response
src/bench: myopic, rule_split, tune
src/match: milp_build, milp_solve(Policy), interface(rollout), mc_value, hindsight
src/vfa: linear, mlp, gnn, train, coef
src/tests: 단계별 테스트
src/result: exp1/2/3 스크립트, plots, summary, runs/(원자료 json)

## 코딩 컨벤션

- 들여쓰기 2칸, 세미콜론 금지
- 주석: 모듈 docstring 3줄, 클래스 docstring 3줄, 함수는 첫 줄 # 주석 1줄
- 변수명은 간결하게
- try/except 금지 (스레딩 동기화 목적 제외)
- 모든 수치는 utils/params.py의 frozen Cfg에만 둔다
- MILP는 gurobipy로 작성한다. 모델 파라미터 OutputFlag=0, Seed·Threads·MIPGap은 Cfg에서 고정한다.
- 솔버 상태는 assert m.Status == GRB.OPTIMAL로 검사한다.
- 재참여확률의 구간선형 근사는 addGenConstrPWL을 사용하고, 꺾인 점 생성은 utils/pwl.py가 담당한다.
- 보고와 설명은 한국어로 한다.

## 검증 과정

- 슬라이스 단위로 구현하고, 슬라이스마다 pytest 통과 + MILP 최적해 확인 후 git commit
- 모든 실행은 conda 환경 apiems의 python으로 한다 (C:\Users\Jiyoon\anaconda3\envs\apiems\python.exe, gurobipy·학술 라이선스 포함)
- 테스트는 src/ 에서 `pytest tests` 로 실행 (apiems 환경: `conda run -n apiems pytest tests` 또는 위 python으로 `python -m pytest tests`)
- 모든 실험 결과는 result/runs/*.json 에만 저장하고, 그림·요약은 그 json에서만 생성
- 진단·비교표도 result/runs/diag_*.json의 반복별 원자료에서만 만든다 (result/diag.py). 보고하는 표마다 출처 파일명을 적는다.

## 주의사항

- 비교군(규칙 기반, 선형 근사, 근시안)을 약하게 만들어 제안 방법이 좋아 보이게 하지 말 것.
  가치 근사 정책의 주 비교 기준은 규칙 기반(최선 비율)이고, 근시안 대비 결과는 참고로만 보고한다.
- 선형과 MLP는 반드시 utils/features.py의 같은 시장 요약 지표만 사용
- 정책 비교는 공통 난수: 재참여는 참여자·기간별 균등난수를 미리 뽑아 u < p(잉여)로 판정
- 모든 정책은 같은 MILP를 공유한다. 가치 근사 정책과 근시안은 유지 가치만 다르고(유지 가치 0 = 근시안),
  규칙 기반은 같은 MILP에서 잉여배분만 고정 비율로 묶는다.
- 시뮬레이션 기준치와 경험 수집은 동일한 후속 정책(규칙 기반, 최선 비율)과 공통 난수로 계산
- 학습용 반응 함수와 평가용 반응 함수를 분리해서 지정 가능하게
- 불리한 결과도 숨기지 말고 기록. 결과에는 소규모 예비 실험임을 명시
- 가설은 검증 대상일 뿐 기대 결과가 아니다. 가설과 반대인 결과도 동등하게 유효한 결과로 기록한다.
- 결과를 보기 전에 실험 설정(규모, 반복 수, 하이퍼파라미터 탐색 범위와 예산)을 고정하고,
  결과를 본 뒤 한쪽 방법에만 유리하게 설정을 바꾸지 않는다.
- 모든 근사 방법(선형, MLP, GNN)에 같은 튜닝 예산을 준다.
- 결과가 예상과 다르면 먼저 구현 오류를 양쪽 모두에 대해 같은 기준으로 점검하고,
  점검 과정과 결론을 그대로 기록한다.