## 프로젝트 개요

주제: 5PL 미들마일 플랫폼의 동적 매칭 및 잉여배분 (APIEMS 2026)
구현 언어: Python

플랫폼은 다품종 묶음 주문을 내는 구매자와 품목별 공급 용량을 가진 공급자를 매칭하고,
단일 허브로 통합 배송한다. 매 기간 (1) 주문 수락, (2) 품목별 공급자·수량 배정,
(3) 구매자·공급자·플랫폼 간 거래잉여 배분을 결정한다. 주문은 요청한 모든 품목의
요구수량이 확보될 때만 성립한다. 참여자의 다음 기간 재참여 확률은 배분받은 잉여에 따라 달라진다.

연구 질문: 현재의 매칭·잉여배분이 미래 참여자 구성을 바꾸는 플랫폼에서,
미래 가치를 어떻게 근사해야 동적 의사결정이 개선되는가?

방법: 시뮬레이션 경험으로 미래 가치 V(s)를 학습(선형/MLP/GNN) →
계수 c_i = V(s) − V(s without i) → MILP 목적함수 = 당기 이윤 + Σ c_i × 재참여확률(구간선형 근사)

- RQ1 (비선형성): 같은 공급 현황 수치(참여자 수, 품목별 공급량, 대체 공급자 수)를 줄 때,
  신경망 근사가 선형 근사보다 미래 가치를 더 정확히 예측하고 장기 성과를 개선하는가? (실험 1)
- RQ2 (연결 구조): 공급 현황 수치에 더해 "어느 공급자가 어떤 품목들을 함께 공급하는지"를
  그래프로 반영하면 개선되는가? 개선되는 공급 구조 조건과 수치만으로 충분한 조건은 무엇인가? (실험 3)
- RQ3 (계수 변환): 학습한 가치가 참여자별 유지 기여도 계수로 변환되어 MILP에 들어간 뒤에도
  그 정보가 결정에 유효하게 반영되는가? (실험 1·3의 변환 오차)
- RQ4 (강건성): 재참여 반응이 학습 시 가정과 다를 때도 성능이 유지되는가? (실험 2)

비교 기준: 근시안 정책(계수 0), 규칙 기반 잉여배분, 선형 가치 근사


## 파일 구조

src/utils: params(Cfg), common(난수 스트림), arrivals, instance, pwl, features
src/env: platform(Env), graph, response
src/bench: myopic, rule_split, tune
src/match: milp_build, milp_solve(Policy), interface(rollout), mc_value, hindsight
src/vfa: linear, mlp, gnn, train, coef
src/tests: 단계별 테스트
src/result: exp1/2/3 스크립트, plots, summary, runs/(원자료 json)

## 코딩 컨벤션

- 들여쓰기 2칸, 세미콜론 금지
- 주석: 모듈 docstring 3줄, 클래스 docstring 3줄, 함수는 첫 줄 # 주석 1줄
- 변수명은 간결하게
- try/except 금지 (스레딩 동기화 목적 제외). 솔버 상태는 assert로 검사
- 모든 수치는 utils/params.py의 frozen Cfg에만 둔다
- MILP는 PuLP로 작성

## 검증 과정

- 슬라이스 단위로 구현하고, 슬라이스마다 pytest 통과 + MILP 최적해 확인 후 git commit
- 테스트는 src/ 에서 `pytest tests` 로 실행
- 모든 실험 결과는 result/runs/*.json 에만 저장하고, 그림·요약은 그 json에서만 생성

## 주의사항

- 비교군(근시안, 규칙 기반, 선형 근사)을 약하게 만들어 제안 방법이 좋아 보이게 하지 말 것
- 선형과 MLP는 반드시 utils/features.py의 같은 특징만 사용
- 정책 비교는 공통 난수: 재참여는 참여자·기간별 균등난수를 미리 뽑아 u < p(잉여)로 판정
- 모든 정책은 같은 MILP를 공유하고 계수만 다름 (계수 0 = 근시안)
- MC 정답 가치는 동일한 후속 정책과 공통 난수로 계산
- 학습용 반응 함수와 평가용 반응 함수를 분리해서 지정 가능하게
- 불리한 결과도 숨기지 말고 기록. 결과에는 소규모 예비 실험임을 명시